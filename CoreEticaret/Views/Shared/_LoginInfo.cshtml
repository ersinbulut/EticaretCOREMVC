<style>
    .dropdown:hover > .dropdown-menu {
        display: block;
        padding-top: 10px;
    }

    .navbar-nav > .user-menu > .dropdown-menu > li.user-header {
        width: 220px;
    }

    .navbar-nav > .user-menu > .dropdown-menu {
        margin-top: -10px;
    }

    }
</style>


@inject Microsoft.AspNetCore.Identity.SignInManager<ApplicationUser> _signInManager

@if (_signInManager.IsSignedIn(User))
{
    <div id="mobile-menu-wrap"></div>
    <div class="offcanvas__auth">
        @* <li class=""><a href="/AdminHome/Index" target="_blank">Admin Paneli</a></li>*@
        <li class="dropdown user user-menu">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <span class="hidden-xs">Hesabım</span>
            </a>
            <ul class="dropdown-menu">
                <li class="user-header" style="padding:5px; background-color:#3A3F44;">
                    <p style="font-size:15px; color:dark;">
                        @(User.FindFirst("UserFirstName").Value + " " + User.FindFirst("UserLastName").Value )
                    </p>
                </li>
                 <li class="user-header" style="padding:5px; background-color:orange;">
                     <a href="/Admin/AdminCategory/Index/">Admin Paneli</a>
                </li>
                 <li class="user-header" style="padding:5px; background-color:red;">
                     <a asp-controller="Account" asp-action="Logout">Cıkış Yap</a>
                </li>
            </ul>
        </li>
    </div>
}
else
{
    @* <li class="nav-item">
        <a asp-controller="Account" asp-action="Login" asp-route-returnUrl="@Context.Request.Path" class="nav-link" style="font-size:10px;">Giriş Yap</a>
        </li>
        <li class="nav-item">
        <a asp-controller="Account" asp-action="Signup" class="nav-link">Kayıt Ol</a>
        </li>*@
    <div id="mobile-menu-wrap"></div>
    <div class="offcanvas__auth">
        <a href="/Account/Login/">Giriş Yap</a>
        <a href="/Account/Signup/">Kayıt Ol</a>
    </div>
    @*  <li class="nav-item">
        <a class="btn btn-outline-primary" asp-controller="Account" asp-action="Login" asp-route-returnUrl="@Context.Request.Path">Login</a>
        <a class="btn btn-primary" asp-controller="Account" asp-action="Signup">Register</a>
        </li>*@
}